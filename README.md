# Valid ID pages

Upload these files to the root of a GitHub repository, then enable **Settings → Pages → Deploy from a branch** using the `main` branch and `/ (root)` folder.

The pages will be available at:

- `/Jeremy/` — Jeremy Lauten
- `/caroline/` — Caroline Elrod
- `/matt/` — Matt Cebedo
- `/maycie/` — Maycie Krajewski

GitHub Pages URLs are case-sensitive. Keep the `Jeremy` folder capitalized.
